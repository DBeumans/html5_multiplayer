class Player
{
  constructor(id, name, x, y, width, height)
  {
    this.id = id;
    this.name = name || "Shithead";
    this.x = x || 0;
    this.y = y || 0;
    this.width = width || 0;
    this.height = height || 0;
  }
}
